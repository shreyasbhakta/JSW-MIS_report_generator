<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.menuforreportdetails">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MenuforReportDetails">
        <activity android:name=".MISReport"></activity>
        <activity
            android:name=".PlannedProduction"
            android:label="@string/title_activity_planned_production"
            android:theme="@style/Theme.MenuforReportDetails.NoActionBar" />
        <activity android:name=".ActualProduction" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>